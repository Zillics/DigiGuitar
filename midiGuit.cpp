#define THRESH 40
#include "midiGuit.h"
//UNTESTED, ToDo
/*
void simplePlay(){
	while(true){
		if(readSlider(2,3) != -1){
		MIDI.sendNoteOn(100,100,1);
			while(readSlider(2,3) != -1){
				sendPitchBend(map(readSlider2(0,1,10),0,2,0,65535),1);
			}
		MIDI.sendNoteOff(100,100,1);
		}
	}
	
}
*/